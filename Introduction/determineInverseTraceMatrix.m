X = [-1 5 6; 7 8 6; -5 1 2];

d = det(X);

I = eye(5);
detI = det(I);      %determine of identity matrix is always 1

invX = inv(X);      %inverse of matrix X

traceX = trace(X);  %trace of matrix X : trace is the sum of the diagonal elements of a matrix